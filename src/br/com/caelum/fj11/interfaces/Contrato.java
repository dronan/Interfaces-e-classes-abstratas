package br.com.caelum.fj11.interfaces;
interface Contrato {
	/* 
	 * interface funciona como um contrato, tudo que nela est‡, deve ser declarado como publico
	 * e constar na classe que a implementa
	 */
	
	void metodo(int variavel);
	
}
